// ViewController.swift
// Retained for storyboard compatibility; app uses SceneDelegate to set rootViewController.

import UIKit

class ViewController: UIViewController {
    override func viewDidLoad() {
        super.viewDidLoad()
        // App entry is handled by SceneDelegate → ProjectGalleryVC
    }
}
